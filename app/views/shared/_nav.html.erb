<!-- <header class="text-gray-200 body-font bg-gray-900">
  <div class="md:container md:mx-auto flex flex-wrap p-5 flex-col md:flex-row justify-center">

    <a href="/" class="flex title-font font-medium text-left md:items-center mb-0 md:ml-0 ml-0 text-gray-200">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="w-10 h-10 text-white p-2 bg-indigo-500 rounded-full" viewBox="0 0 24 24">
        <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
      </svg>
      <span class="ml-3 text-xl text-gray-200">Aus JDM Parts</span>
    </a>

    <input type="checkbox" id="menu-toggle" class="hidden">
        <label for="menu-toggle" class="nav-btn lg:hidden">
        <i></i>
        <i></i>
        <i></i>
    </label>

    <nav class="menu absolute md:relative top-20 md:top-0 lg:left-0 md:ml-auto z-20 flex flex-col md:flex-row md:space-x-6 font-semibold w-full md:w-auto bg-gray-900 md:rounded-none text-center md:shadow-none p-6 pt-0 md:p-0 h-screen lg:h-full transition duration-500 ease-in-out">
        <%= link_to "Parts", listings_path, class: 'flex justify-center items-center hover:text-gray-300 border-b-2 border-transparent hover:border-indigo-500 cursor-pointer transition-all ease duration-500 my-2' %>
        <%= link_to "Users", users_path, class: 'flex justify-center items-center hover:text-gray-300 border-b-2 border-transparent hover:border-indigo-500 cursor-pointer transition-all ease duration-500 my-2' %>

        <% if user_signed_in? %>
            <%= link_to "Profile", user_path(current_user), class: 'flex justify-center items-center hover:text-gray-300 border-b-2 border-transparent hover:border-indigo-500 cursor-pointer transition-all ease duration-500 my-2' %>
        <% else %>
            <%= link_to "Sign In", new_user_session_path, class: 'flex justify-center items-center hover:text-gray-300 border-b-2 border-transparent hover:border-indigo-500 cursor-pointer transition-all ease duration-500 my-2' %>
            <%= link_to "Sign Up", new_user_registration_path, class: 'flex justify-center items-center inline-flex items-center bg-gray-200 hover:bg-gray-300 border-0 py-1 px-3 focus:outline-none hover:bg-gray-200 rounded text-base mt-4 md:mt-0 text-gray-900' %>
        <% end %>
        <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, class: 'flex justify-center items-center mr-5 hover:text-gray-300 border-b-2 border-transparent hover:border-indigo-500 cursor-pointer transition-all ease duration-500 my-2', method: :delete %>
        <% end %>
    </nav>

  </div>
</header> -->

<div class="wrapper">
    <header>
        <nav id="navbar" class="nav bg-gray-900"">
            <input type="checkbox" id="nav" class="hidden">
            <label for="nav" class="nav-btn">
                <i></i>
                <i></i>
                <i></i>
            </label>
            <div class="logo">
                <a href="/"><%= image_tag("logo.png") %></a>
            </div>
        
            <div class="nav-wrapper bg-gray-900">
                <ul>
                    <li><a href="../" class="lg-hidden brand"><%= image_tag("logo.png") %></a></li>
                    <li><a class="link border-b-2 border-transparent hover:border-indigo-500" href="/">home</a></li>
                    <li><a class="link border-b-2 border-transparent hover:border-indigo-500" href="/listings">listings</a></li>

                    <% if user_signed_in? %>
                        <li><a class="link border-b-2 border-transparent hover:border-indigo-500" href="<%= user_path(current_user) %>">Profile</a></li>
                    <% else %>
                        <li><a class="link border-b-2 border-transparent hover:border-indigo-500" href="/sign_in">Sign In</a></li>
                        <li><a class="link btn bg-indigo-500 rounded hover:bg-indigo-600" href="/sign_up">Sign Up</a></li>
                    <% end %>
                    <% if user_signed_in? %>
                        <li><a class="link border-b-2 border-transparent hover:border-indigo-500" href="/sign_out" data-method="delete">Sign Out</a></li>
                    <% end %>
                    <li>
                        <button id="myBtn" class="text-white">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                            </svg>
                        </button>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
</div>

<div id="myModal" class="modal">

  <!-- Modal content -->
  <div class="modal-content">
    <span class="close">&times;</span>
    <p class="text-2xl">Shohping Cart</p>
    <% if @cart.present? %>
    <% else %>
        <p>Your Shopping Cart Is Empty</p>
    <% end %>

    <% @cart.each do |listing| %>
        <div class="flex items-center mt-2">
            <%= listing.name %>
            <%= button_to "Remove from cart", remove_from_cart_path(listing), method: :delete, class: 'ml-2 flex text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded cursor-pointer' %>
        </div>
    <% end %>

    <% if @cart.present? %>
        <%= button_to "Buy", checkout_create_path, class: 'mt-2 flex text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded' %>
    <% end %>
    <%= @cart.collect { |item| item.to_builder.attributes! } %>
  </div>

</div>

<script>
// Get the modal
var modal = document.getElementById("myModal");

// Get the button that opens the modal
var btn = document.getElementById("myBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks the button, open the modal 
btn.onclick = function() {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
</script>